def binary(number):
    if number ==  0:
        return('0')
    else:
        modulo = ''
    while number > 0:
        modulo = str(number % 2) + modulo
        number = number // 2
    return(modulo)
    
